# simpomo

## What this is
シンプルで軽量なポモドーロタイマーのデスクトップアプリ。起動すると画面の隅（デフォルト右上、
設定で位置変更可）に小さく表示され、基本は常に最前面（always-on-top）。ボタン一つで最前面モードを
切り替えられる。作業/休憩時間・サイクル数・通知音・ウィンドウサイズを設定でカスタマイズできる。
形態: クロスプラットフォームのデスクトップアプリ（Tauri）。

## Why it exists
作業に集中するためのポモドーロ・テクニックを、邪魔にならず常に視界の隅に置けるツールが欲しい。
既存のポモドーロアプリは多機能・重量級だったり広告/アカウント必須だったりするものが多いので、
「本当に軽くて、最前面に小さく出るだけ」のものを自分の理想形で作り切る。公開もする。

## Project Type
- ソフトウェア開発（コードを書いて完成させる）

## Stack
- **フレームワーク**: Tauri v2（クロスプラットフォーム・軽量デスクトップ）
- **バックエンド/コアロジック**: Rust（タイマー進行・サイクル状態管理）
- **フロントエンド (UI)**: Svelte + Vite + TypeScript
- **パッケージマネージャ**: pnpm（フロント）／ cargo（Rust）
- **音再生**: WebView の HTML5 Audio（クロスプラットフォーム）
- 動作環境: クロスプラットフォーム（Windows / macOS / Linux）。
  開発主環境は Windows・WQHD (2560x1440)。ウィンドウサイズは解像度を加味して算出する。

## Definition of Done
- 列挙した全機能（下記 spec 参照）が動作する。
- Windows で動作確認でき、Tauri bundler で各 OS 向けインストーラ/実行ファイルが生成できる。
- GitHub Releases にインストーラを配布できる状態。
- コアロジック（タイマー進行・サイクル遷移）の単体テストが pass する。

### テスト方針
- **コアロジックのみ単体テスト**（Rust 側）: タイマーのカウントダウン、作業→休憩→次サイクルの
  状態遷移、サイクル数（0=手動停止 / 有限 N / 無限）の扱い、設定値の境界。
- UI・通知音・最前面トグルなどは手動確認でよい。

### 成功指標
- 自分が日常的に使い続けられること。
- 軽量であること（重量級依存を持ち込まない。バイナリ/メモリを小さく保つ）。

## Don't
- **公開リポジトリ**なので機密情報を一切コミットしない。
  特に Tauri の **コード署名鍵 / updater の秘密鍵 / `.env`** は絶対にコミットしない。
- **著作権のある音源を同梱しない**。通知音はロイヤリティフリー or 自作のもののみ。
  （ライセンス表記が必要なものは出典を `docs/` に記録）
- Electron など重量級スタックは不採用（軽量要件に反する）。

## External Services
- なし（オフライン完結を前提）。
- 将来オートアップデータを導入する場合のみ署名鍵が必要。その鍵はリポジトリ外
  （OS のキーチェーン / CI のシークレット）で管理し、本ファイルにも書かない。

## Stakeholders
- 主に作者本人（自分用ツール）。
- 公開リポジトリのため、ソースを見る/使う不特定の利用者も想定。

## When working on...
- 機能仕様 → `docs/spec.md`
- 技術選定の経緯（なぜ Tauri/Svelte か） → `docs/decisions/0001-initial-stack.md`
- ローカル開発・ビルド・テスト手順 → `docs/development.md`
- 設計判断の履歴 → `docs/decisions/`
- ドメイン用語が増えたら → `docs/glossary.md` を作って記録する（現状は不要なため未作成）

## Commands
<!-- プロジェクト初期化後に埋める。想定（確定したら更新）:
- フロント依存: `pnpm install`
- 開発起動: `pnpm tauri dev`
- ビルド: `pnpm tauri build`
- Rust テスト: `cargo test`（src-tauri 配下）
-->

## Notes
- サイクル数 = 自動継続するサイクル数。0=1セットで停止し手動 start 待ち / 有限 N=N セット自動 / 無限=自動継続。詳細は spec 参照。
- 作者から「Claude 系ファイルは公開リポに不要かも」との意向あり。`.claude/` は `.gitignore` 済み。
  CLAUDE.md 自体を公開リポに含めるか/除外するかは作者判断（init-session.md 参照）。
